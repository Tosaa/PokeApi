package redtoss.app.poke.ui.common

// This module could contain the implementation for desktopMain and androidMain,
// but if they differ, its better to implement each of them.

/*
actual fun initPlatform() {
    Logger.loggingFunction = {message->"main(jvm): $message"}
}
*/